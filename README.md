# Fashion-MNIST-Neural-Network

## Overview
This project implements a neural network using the Fashion MNIST dataset for classifying grayscale images of 10 clothing types. Built with Keras and TensorFlow, the model includes 3 hidden layers.
